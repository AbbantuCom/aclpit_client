export default function Home() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-ivory">
      <div className="text-center px-6">
        <span className="inline-block text-wine text-sm font-bold tracking-[0.3em] uppercase mb-4">
          ACLPIT
        </span>
        <h1 className="text-ink text-3xl sm:text-4xl font-bold">
          Next.js scaffold ready — pages land in Phase 2
        </h1>
      </div>
    </main>
  );
}
